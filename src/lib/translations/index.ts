import { en } from './en';
import { fr } from './fr';
import { pidgin } from './pidgin';

// Legacy export for backward compatibility
export const translations = {
  en,
  fr,
  pidgin
};

// New text object for unified language system
export const text = {
  en,
  fr,
  pidgin
};
